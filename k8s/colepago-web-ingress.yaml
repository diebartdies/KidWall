apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: colepago-web-ingress
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: colepago-web.srsrv.net.ar
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: colepago-service
            port:
              number: 80
  tls:
  - hosts:
    - colepago-web.srsrv.net.ar
    secretName: colepago-web-srsrv-tls
